export default {
  plugins: {
    // Use the official Tailwind PostCSS plugin package (separate package)
    // The PostCSS plugin package name is '@tailwindcss/postcss'
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};
